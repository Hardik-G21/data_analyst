Day 3 Progress Report
Setup Status
All required tools are now successfully installed and operational:

- VS Code is installed and configured with necessary extensions.
- Python is installed and working properly via terminal and VS Code.
- Node.js is installed and verified using `node -v`.
- Git is installed and basic commands are functioning.
- MySQL Workbench is installed and connected to the local database.

Environment is fully functional and ready for development tasks.

Task Inventory
Today, I completed the following tasks:

- Set up development environment (VS Code, Python, Node.js, Git)
- Installed required Python libraries
- Created and structured a basic project workspace
- Learned how to run Python files inside VS Code
- Created a sample dataset in Excel/Google Sheets
- Performed basic spreadsheet calculations (SUM, AVERAGE)
- Installed MySQL and MySQL Workbench
- Attempted database connection and basic queries
- Explored SQL table creation and insertion commands

Debugging Log
1. Pip Not Recognized Error
Problem:
While trying to install Python packages, the terminal showed:
`pip: The term 'pip' is not recognized...`

Solution:
- Verified Python installation using `python --version`
- Added Python and Scripts folder to Environment Variables (PATH)
- Restarted terminal
- Confirmed fix using `pip --version`

2. MySQL Workbench Connection Issue
Problem:  
Unable to connect MySQL Workbench to local server.

Solution:  
- Ensured MySQL Server was running via services
- Verified correct username (root) and password
- Checked port number (default 3306)
- Reconfigured connection settings in Workbench
- Successfully established connection after correction

Key Insights
- I understood how environment variables (PATH) play a crucial role in running tools globally from the terminal.
- Debugging is not about avoiding errors, but systematically identifying and fixing them.
- I realized that tools like MySQL Workbench require both proper installation AND correct configuration to work.
- Small setup issues can block progress, but once solved, they significantly boost productivity.

Aha! Moment:
"I finally understood that most errors are not complex—they just require patience and step-by-step troubleshooting.

#Conclusion
Day 3 was focused on building a solid technical foundation. Despite initial setup challenges, all issues were resolved, and the development environment is now fully ready for future tasks.